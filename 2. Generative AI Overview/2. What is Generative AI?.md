Generative AI is a specialized field of artificial intelligence. Unlike traditional AI that analyzes or predicts, Generative AI focuses on creating entirely new and realistic content, such as text, images, or data, by learning from existing examples.

<i>At its core, generative AI is a specialized subset of artificial intelligence that focuses on creating new content or data that is novel and realistic. 
![]()Unlike traditional AI, which might analyze new data or make predictions, generative models use existing data to generate new, unseen outputs. It's about building systems that learn the underlying patterns and structures of existing data. For instance, from books, images, and artwork. We then use that understanding to produce something new. This breakthrough allows AI systems to move beyond just understanding or predicting and become tools in the hands of adept users to create new works and innovate. How does GenAI fit within the realm of AI or artificial intelligence? At its highest level, AI encompasses machines that perform tasks mimicking human behavior, such as problem solving, decision making, and understanding natural language. Within that, you may have heard of Machine Learning or ML, which allows computers to get better at a task without explicit programming. If we use what are called artificial neural networks which are inspired by the human brain, we get deep learning, a subset of ML. Generative AI leans on Deep Learning, and it is the newest, most talked about subset of AI, specifically designed to create novel and realistic content, often using natural language to guide it. The real power behind the recent explosion of generative AI lies in foundation models. These are massive AI systems trained on huge amounts of data. Unlike traditional Machine Learning engineers who build models from scratch, so called AI engineers leverage these existing foundation models, focusing less on training them and more on adapting them to specific tasks. For example, summarization of documents, answering questions using these documents or data extraction, you guessed it from these documents. These models are trained through a process called self supervision, where they predict parts of their input data, which can consist of large amounts of data obtained from the web and other sources, solving the data labeling bottleneck that held AI back for years. As these models scaled up, they evolve from simple language models to large language models, LLMs for short, and even multimodal models, for instance, vision language models or VLMs, capable of handling images in video alongside text. You may imagine an LLM as a super smart auto correct. It uses statistics to predict the next most probable word, but on a grand world knowing scale.</i>

At its core, Generative AI is a specialized subset of artificial intelligence. Its primary focus is on creating new content or data that is both novel and realistic.

Unlike traditional AI, which might be used to analyze data or make predictions, generative models use existing data to generate completely new, unseen outputs. The goal is to build systems that learn the underlying patterns and structures from existing data, such as books, images, or artwork, and then use that understanding to produce something new.

This breakthrough allows AI to move beyond simply understanding or predicting. It transforms AI into a creative tool that adept users can leverage to innovate and create new works.

Generative AI fits into the broader field of AI as its newest subset. The hierarchy starts with **Artificial Intelligence (AI)**, the broad concept of machines mimicking human tasks like problem-solving or decision-making. Within AI is **Machine Learning (ML)**, which allows computers to improve at tasks without explicit programming. A subset of ML is **Deep Learning**, which uses artificial neural networks inspired by the human brain. Finally, **Generative AI** leans on deep learning to create its novel and realistic content, often guided by natural language.

Generative AI is a subset of Deep Learning, which is a subset of Machine Learning, which is a subset of Artificial Intelligence
Generative AI is a small subset of Artificial Intelligence

The recent explosion in Generative AI is powered by foundation models. These are massive AI systems trained on huge amounts of data. Instead of building models from scratch, AI engineers now leverage these existing models, focusing more on adapting them for specific tasks rather than training them.

Foundation Models (including LLMs and VLMs) can be used for a number of downstream tasks, such as summarization, answering questions, and data extraction
The Many Uses of Foundation Models

Common tasks for these adapted models include summarization of documents, answering questions based on those documents, or extracting specific data from them.

Foundation models are trained using self-supervision. In this process, the model predicts parts of its own input data, which is often sourced in large amounts from the web. This method solved the "data labeling bottleneck" that previously slowed AI development. As these models scaled, they evolved from simple language models into Large Language Models (LLMs) and even multimodal models (like Vision Language Models, or VLMs) that can handle images and video alongside text.

A simple way to think of an LLM is as a "super, super smart auto-correct." It uses statistics to predict the next most probable word, but it does so on a massive, world-knowing scale.

> Generative AI is a subset of deep learning that uses massive, self-supervised foundation models (like LLMs) to create new, realistic content rather than just analyzing existing data.


